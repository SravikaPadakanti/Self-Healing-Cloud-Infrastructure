# Self-Healing-Cloud-Infrastructure
Automated, fault-tolerant AWS cloud architecture that leverages EC2, Auto Scaling, ELB, and CloudWatch to detect server failures, auto-replace degraded instances, and dynamically scale resources.
